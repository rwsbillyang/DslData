package com.github.rwsbillyang.dsldata.http

object DefaultClientConfig {
    const val connectTimeoutMs = 20 * 1000L
    const val readTimeoutMs = 10 * 1000L
    const val writeTimeoutMs = 10 * 1000L
}